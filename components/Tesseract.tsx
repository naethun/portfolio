'use client';

// Tesseract animation intentionally stubbed out.
// Hero renders a blank canvas; component preserved so call sites still type-check.
export default function Tesseract() {
  return null;
}
